---
created: 2025-05-21T15:15
updated: 2025-05-21T17:55
---

# **无人机小目标检测系统学习与部署计划 (Todo 清单)**

## **一、近期已完成/进行中任务回顾

*   **Drone_FasterRCNN项目**：
    *   [x] 已完成从无人机视角图像中检测库的部署工作。
*   **VisDrone数据集处理**：
    *   [x] 已完成目标检测模型训练。
    *   [x] 已完成标注转换为PASCAL-VOC格式。
    *   [x] 已跑通工具包并录制视频。
    *   [ ] 待完成：可视化和EDA（探索性数据分析）。
*   **YOLO系列算法**：
    *   [x] 已关注YOLOv8n-ACW库。
    *   [x] 已部署Ultralytics YOLOv8。
    *   [x] 已开发基于PyQt5的图形界面（支持图片、视频、摄像头输入），并集成了参数调整和结果可视化。
*   **数据集标注**：
    *   [x] 已安装LabelMe，支持数据集标注和模型训练。

## **二、核心学习与实践任务 (下一步重点：深入源码与改进模型)**

根据林云的指示，当前最核心的任务是深入理解和修改YOLOv8及其改进模型的源码，确保能改代码。

1.  **YOLOv8基础知识深入与源码熟悉**
    *   **目标**：熟练掌握YOLOv8的训练、推理、评估流程，并能进行基本的代码修改，特别是理解配置文件和模块。
    *   **具体步骤**：
        *   [x] 环境搭建与Ultralytics YOLOv8部署。
        *   [x] 官方示例运行（训练、推理、验证）。
        *   [ ] **配置文件解析**：深入理解`ultralytics/cfg/models/v8/yolov8n.yaml`和`ultralytics/cfg/datasets/coco128.yaml`，逐行理解每个参数的含义，并尝试修改以适应自定义数据集。
        *   [ ] **YOLOv8模块源码熟悉**：理解YOLOv8的骨干网络、Neck和Head部分的模块实现，为后续修改打下基础。

2.  **HIC-YOLOv8原理与实现 (深入源码)**
    *   **目标**：能够独立地将CBAM、Involution和额外小目标预测头集成到YOLOv8中，并进行训练验证。
    *   **具体步骤**：
        *   [ ] **CBAM模块集成**：
            *   原理回顾CBAM的通道注意力和空间注意力机制。
            *   在`ultralytics/nn/modules/block.py`中，实现CBAM模块的Python类。
            *   在模型配置文件中找到骨干网络（`backbone`）部分，在合适的卷积层之后插入CBAM模块。
            *   验证修改后的模型是否能正常运行。
        *   [ ] **Involution层集成**：
            *   原理回顾Involution与传统卷积的区别。
            *   在`ultralytics/nn/modules/block.py`中，实现Involution层的Python类。
            *   在模型配置文件中骨干网络和Neck连接处插入Involution层。
            *   验证修改后的模型。
        *   [ ] **额外小目标预测头**：
            *   原理回顾P2层（160x160）如何提供更精细的特征图。
            *   在模型配置文件`head`部分，添加一个新的检测头，使其接收来自骨干网络更早的、分辨率更高的特征图（例如，P2层输出）。
            *   可能需要修改`ultralytics/nn/modules/head.py`中的`Detect`类。
            *   调整锚框设置，为小目标头分配更小的锚框。
            *   在VisDrone等小目标数据集上训练，对比原始YOLOv8的mAP@0.5和mAP@0.5:0.95，特别是对小目标的检测性能。
        *   [ ] **代码管理**：使用Git进行版本控制，每次修改都提交，方便回溯。

3.  **Drone-YOLO原理与实现 (深入源码)**
    *   **目标**：能够独立地将RepVGG、三明治融合模块和P2层集成到YOLOv8中，并进行训练验证。
    *   **具体步骤**：
        *   [ ] **代码获取与分析**：
            *   克隆Drone-YOLO的GitHub仓库。
            *   仔细阅读其`models/yolo.py`、`models/common.py`（或`ultralytics/nn/modules/block.py`）和`cfg/models/v8/yolov8-d.yaml`等文件。
            *   重点分析`RepVGGBlock`的实现，以及如何在模型配置文件中引用它。
            *   分析三明治融合模块（如果其代码是独立的模块）的实现和集成方式。
            *   分析P2层（160x160）的添加方式。
        *   [ ] **模块复现与集成**：
            *   尝试在自己的YOLOv8代码库中，独立实现或移植RepVGGBlock和三明治融合模块。
            *   修改YOLOv8的配置文件，将骨干网络中的部分卷积替换为RepVGGBlock。
            *   修改Neck和Head，添加P2层并调整相应的连接。
        *   [ ] **训练与评估**：在VisDrone数据集上训练Drone-YOLO，并与原始YOLOv8以及HIC-YOLOv8进行性能对比，特别关注小目标检测的性能提升。

## **三、后续部署与优化任务 (待启动)**

在完成核心学习和模型改进后，以下是部署和持续优化的计划。

1.  **VisDrone数据集可视化和EDA**：
    *   [ ] 对VisDrone数据集进行深入的可视化和探索性数据分析，理解数据分布、目标大小、场景特点等，为模型训练和优化提供依据。

2.  **模型评估与比较**：
    *   [ ] 在独立的测试集上对改进后的HIC-YOLOv8和Drone-YOLO进行最终评估，计算mAP、F1-score、召回率、精确率等指标。
    *   [ ] 进行小目标专项评估（针对不同大小的目标）。
    *   [ ] 与原始YOLOv8及其他相关模型进行性能比较，为论文提供数据支持。
    *   [ ] 进行推理速度和内存占用测试。

3.  **论文撰写**：
    *   [ ] 整理实验结果、分析模型改进的有效性，撰写关于无人机小目标检测算法的论文。

4.  **部署流程 (实践到生产步骤)**：
    *   [ ] **环境准备与数据管理**：生产环境硬件选型、操作系统与驱动、Python环境、数据管道建立。
    *   [ ] **模型训练与版本管理**：数据集划分、训练策略（迁移学习、超参数优化、数据增强、早停）、模型版本管理。
    *   [ ] **模型评估与生产就绪性测试**：离线评估、鲁棒性测试、生产就绪性检查。
    *   [ ] **模型部署与服务化**：模型导出（ONNX/TensorRT/OpenVINO）、API服务化（Flask/FastAPI，推理引擎集成，容器化）、部署策略（裸机/K8s/云服务/边缘部署）。
    *   [ ] **监控、维护与持续集成/持续部署 (CI/CD)**：实时监控、告警机制、模型再训练与更新、CI/CD流程自动化。
